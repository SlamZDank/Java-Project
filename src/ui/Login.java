package ui;
import javax.swing.JOptionPane;

// A login UI that prompts the user to login using ID only
// If the ID is equal to any number -> Enter usermode -> use user.java
// Else if the ID is equal to "rootUser" -> Enter adminmode -> use admin.java
// If the creds are wrong -> Summon Error window just by invoking the writeErr method with a String in
// Err.java

public class Login {
    
}
