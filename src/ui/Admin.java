package ui;

/*
 * Implement Frameworks that makes it easy to create UI elements
 * Interface directly with the database
 * Show fields
 * Implement a sorting algorithm to sort all scores (from highest to lowest)
 * Export the file to CSV or PDF depending on the libraries included
 */

public class Admin {
    
}
