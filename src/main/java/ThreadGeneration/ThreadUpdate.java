package ThreadGeneration;

public class ThreadUpdate extends Thread {
    // We will use a socket and look out for updates
    // when the user submits.
    // the user sends un update signal to the thread of the admin mode
    // upon clicking on submit,
    // if the admin mode is open, the table will update in real time

    // we will use sockets for this one
}
